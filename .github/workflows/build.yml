name: Bunny Models - Build Android

on:
  workflow_dispatch:

  push:
    branches:
      - main

jobs:
  build-android:
    name: Build APK Android
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      # 1. Baixa o projeto do GitHub
      - name: Checkout
        uses: actions/checkout@v5

      # 2. Configura Node
      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24

      # 3. Cria package.json automaticamente
      - name: Create package.json
        shell: bash
        run: |
          cat > package.json <<'EOF'
          {
            "name": "bunny-models",
            "version": "1.0.0",
            "private": true,
            "main": "node_modules/expo/AppEntry.js",
            "scripts": {
              "start": "expo start",
              "android": "expo start --android",
              "doctor": "expo-doctor"
            },
            "dependencies": {
              "@expo/vector-icons": "^15.0.0",
              "@react-native-async-storage/async-storage": "2.2.0",
              "expo": "~57.0.0",
              "react": "19.2.3",
              "react-native": "0.86.0"
            }
          }
          EOF

      # 4. Cria configuração Expo
      - name: Create app.json
        shell: bash
        run: |
          cat > app.json <<'EOF'
          {
            "expo": {
              "name": "Bunny Models",
              "slug": "bunny-models",
              "version": "1.0.0",
              "orientation": "portrait",
              "userInterfaceStyle": "light",
              "android": {
                "package": "com.bunnymodels.app",
                "versionCode": 1
              },
              "ios": {
                "bundleIdentifier": "com.bunnymodels.app",
                "buildNumber": "1"
              }
            }
          }
          EOF

      # 5. Cria configuração EAS
      # Preview = APK instalável diretamente
      - name: Create eas.json
        shell: bash
        run: |
          cat > eas.json <<'EOF'
          {
            "cli": {
              "version": ">= 16.0.0"
            },
            "build": {
              "preview": {
                "distribution": "internal",
                "android": {
                  "buildType": "apk"
                }
              },
              "production": {
                "autoIncrement": true,
                "android": {
                  "buildType": "app-bundle"
                }
              }
            }
          }
          EOF

      # 6. Confere se o App.js realmente está no repositório
      - name: Check App.js
        shell: bash
        run: |
          if [ ! -f "App.js" ]; then
            echo "ERRO: App.js não encontrado na raiz do repositório."
            exit 1
          fi

          echo "App.js encontrado."
          wc -l App.js

      # 7. Instala dependências
      - name: Install dependencies
        run: npm install

      # 8. Configura Expo + EAS e autentica
      - name: Setup Expo / EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}
          packager: npm

      # 9. Mostra versões para facilitar debug
      - name: Versions
        shell: bash
        run: |
          node --version
          npm --version
          eas --version
          npx expo --version

      # 10. Valida dependências Expo
      - name: Expo dependency check
        run: npx expo install --check

      # 11. Verifica configuração
      - name: Show Expo config
        run: npx expo config --type public

      # 12. Build do APK no EAS
      - name: Build APK
        run: |
          eas build \
            --platform android \
            --profile preview \
            --non-interactive \
            --no-wait
